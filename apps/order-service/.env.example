# Order Service — run from repo root
PORT=5002
CORS_ORIGIN=http://localhost:8000

# Swagger (Try-it-out requests go through the gateway)
SWAGGER_ENABLED=true
SWAGGER_TITLE=RashPulse Order Microservice
SWAGGER_DESCRIPTION=Order placement aur tracking endpoints
SWAGGER_VERSION=1.0.0
SWAGGER_SERVER_URL=http://localhost:8000/api/v1/orders
